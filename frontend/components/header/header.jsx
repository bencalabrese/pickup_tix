var React = require('react');

var Header = React.createClass({
  render: function() {
    return (
      <header className="header">
        I'm a header
      </header>
    );
  }

});

module.exports = Header;
