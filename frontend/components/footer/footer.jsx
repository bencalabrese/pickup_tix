var React = require('react');

var Footer = React.createClass({

  render: function() {
    return (
      <footer className="footer">
        I'm a footer
      </footer>
    );
  }
});

module.exports = Footer;
