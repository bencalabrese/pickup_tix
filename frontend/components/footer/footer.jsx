var React = require('react');

var Footer = React.createClass({

  render: function() {
    return (
      <footer>
        <a href="#">About</a>
        <a href="#">GitHub</a>
        <a href="#">LinkedIn</a>
        <a href="#">Contact</a>
      </footer>
    );
  }
});

module.exports = Footer;
